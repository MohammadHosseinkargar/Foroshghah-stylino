# Copy this file to `.env` next to `deploy/docker-compose.prod.yml` (or export vars in your shell).
#
# SQL Server SA password rules:
# - at least 8 chars
# - must include uppercase, lowercase, numbers and a symbol
MSSQL_SA_PASSWORD="ChangeMe!12345"

# Optional: used by `deploy/docker-compose.prod.yml` to build DATABASE_URL for Docker
DB_NAME="stylino_db"
DB_USER="stylino"
DB_PASSWORD="ChangeMe!Stylino123"

