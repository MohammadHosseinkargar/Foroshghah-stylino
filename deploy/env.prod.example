# Copy this file to `.env` next to `deploy/docker-compose.prod.yml` (or export vars in your shell).
#
# MySQL root password (used by the MySQL container)
MYSQL_ROOT_PASSWORD="ChangeMe!StylinoRoot123"

# Used by `deploy/docker-compose.prod.yml` to create the DB + app user
DB_NAME="stylino_db"
DB_USER="stylino"
DB_PASSWORD="ChangeMe!Stylino123"

